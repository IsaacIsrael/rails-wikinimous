<%= render 'alert', article: @notice %>

<% @articles.each do |article| %>
  <article>
    <h2>
      <%=link_to article.title, article_path(article)%>
    </h2>
    <p><%= article.content.first(101) %></p>
  </article>
<% end %>
